include_directories(
        ${GLEXT_INCLUDE_DIR}
        ${OPENGL_INCLUDE_PATH}
)

link_libraries(
        ${EGL_LDFLAGS}
        piglitutil_gles3
        )

piglit_add_executable (ext_yuv_target-simple simple.c)

if(PIGLIT_BUILD_DMA_BUF_TESTS)
        include_directories(
                ${LIBDRM_INCLUDE_DIRS}
        )

        piglit_add_executable (ext_yuv_target-nv12-sample-y2y
                execution/nv12-sample-y2y.c)
        piglit_add_executable (ext_yuv_target-nv12-render-yuv
                execution/nv12-render-yuv.c)
        piglit_add_executable (ext_yuv_target-nv12-blit-roundtrip
                execution/nv12-blit-roundtrip.c)
        piglit_add_executable (ext_yuv_target-nv12-blit-yuv-to-yuv
                execution/nv12-blit-yuv-to-yuv.c)
endif()
