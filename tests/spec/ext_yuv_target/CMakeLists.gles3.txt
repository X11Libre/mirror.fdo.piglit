include_directories(
        ${GLEXT_INCLUDE_DIR}
        ${OPENGL_INCLUDE_PATH}
)

link_libraries(
        ${EGL_LDFLAGS}
        piglitutil_gles3
        )

piglit_add_executable (ext_yuv_target-simple simple.c)
