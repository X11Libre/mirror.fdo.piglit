include_directories(
	${GLEXT_INCLUDE_DIR}
	${OPENGL_INCLUDE_PATH}
)

link_libraries (
	piglitutil_${piglit_target_api}
	${OPENGL_gl_LIBRARY}
)

piglit_add_executable (ext_multisampled_render_to_texture-clear_color_and_depth clear_color_and_depth.c)

# vim: ft=cmake:
